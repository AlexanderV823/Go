# Проектная работа «Мини-приложение для управления задачами на Go»

Консольное приложение поддерживает следующие команды:
1. [] add → Добавить задачу (флаг --desc)  
   Пример вызова: -desc="Купить продукты"  
   При добавлении новой задачи генерируется и сообщается пользователю ее уникальный номер (id) в диапазоне от 1 до 250.  
2. [] list → Вывести задачи (флаг --filter=all/done/pending, значение по умолчанию all)
   Пример вызова: --filter=pending  
   Последовательно выводит все задачи, согласно указанному фильтру.
3. [] complete → Отметить задачу выполненной (флаг --id, значение по умолчанию 0)    
   Пример вызова: complete --id=2  
   Осуществляется поиск по всему списку загруженных и созданных задач. Задаче с найденным id присваивается отметка о выполнении. Если задачи с таким id нет, выводится соотщение пользователю.
4. [] delete → Удалить задачу (флаг --id, значение по умолчанию 0)    
   Пример вызова: delete --id=3  
   Осуществляется поиск по всему списку загруженных и созданных задач. Задача с указанным id удаляется из списка. Если задачи с таким id нет, выводится соотщение пользователю.
5. [] export → Экспорт в CSV/JSON (флаги --format, значение по умолчанию json, --out, значение по умолчанию tasks.json)  
6. [] load → Импорт из CSV/JSON (флаг --file, значение по умолчанию tasks.json)  
   Пример вызова: load --file=mytasks.csv  
   По умолчанию при запуске программы загружается список задач из файла tasks.json, расположенного в папке MyTasks, находящейся в домашнем каталоге текущего пользователя. Если такого файла или папки не существует, то они будут созданы. Файл для загрузки необходимо предварительно переместить в папку MyTasks.